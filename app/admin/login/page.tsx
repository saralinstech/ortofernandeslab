import { getAdminAccess } from "../../admin-auth";import { redirect } from "next/navigation";
export const dynamic="force-dynamic";
export default async function AdminLogin(){if(await getAdminAccess())redirect("/admin");return <main className="admin-login"><section><div className="login-logo"><img src="/logo-orto.jpg" alt="Logo do Laboratório Orto Fernandes"/></div><span className="kicker">ACESSO RESTRITO</span><h1>Área administrativa</h1><p>Entre com as credenciais fornecidas pela Orto Fernandes.</p><form action="/api/admin-login" method="post"><label>E-mail<input name="email" type="email" required autoComplete="username"/></label><label>Senha<input name="password" type="password" required autoComplete="current-password"/></label><button className="admin-primary">Entrar no painel</button></form><a href="/">← Voltar para o site</a></section></main>}
