import { env } from "@/platform-env";import { getChatGPTUser } from "../../chatgpt-auth";
type MediaBucket={put(key:string,value:ArrayBuffer,options:{httpMetadata:{contentType:string}}):Promise<unknown>};
export async function POST(request:Request){const user=await getChatGPTUser();if(!user)return Response.json({error:"Não autorizado"},{status:401});const form=await request.formData();const file=form.get("file");if(!(file instanceof File))return Response.json({error:"Imagem obrigatória"},{status:400});if(!file.type.startsWith("image/")||file.size>8_000_000)return Response.json({error:"Use uma imagem de até 8 MB"},{status:400});const key=`${Date.now()}-${crypto.randomUUID()}`;const bucket=(env as unknown as {MEDIA:MediaBucket}).MEDIA;if(!bucket)return Response.json({error:"Armazenamento indisponível"},{status:503});await bucket.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return Response.json({url:`/api/media/${key}`},{status:201})}
