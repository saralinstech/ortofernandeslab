import { cookies } from "next/headers";import { eq } from "drizzle-orm";import { getDb } from "../../../db";import { adminSessions } from "../../../db/schema";
export async function GET(request:Request){try{const jar=await cookies(),id=jar.get("of_admin_session")?.value;if(id)await getDb().delete(adminSessions).where(eq(adminSessions.id,id));const response=Response.redirect(new URL("/admin/login",request.url),303);response.headers.append("Set-Cookie","of_admin_session=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0");return response}catch{return Response.redirect(new URL("/admin/login",request.url),303)}}
